# Core Django
# FIXED: Django 6.x does not exist — latest LTS is 5.2
Django==5.2.3
asgiref==3.8.1
sqlparse==0.5.3

# Database
mysqlclient==2.2.4
dj-database-url==2.2.0

# REST API
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-filter==24.3

# Production Server
gunicorn==22.0.0
whitenoise==6.7.0

# Image handling
pillow==10.4.0

# Environment
python-dotenv==1.0.1

# Utilities
requests==2.32.3
user-agents==2.2.0

# Cache (Redis — required for 20K+ daily traffic, shared across Gunicorn workers)
# Set REDIS_URL=redis://localhost:6379/0 in production .env to activate
redis>=5.0.0
django-redis>=5.4.0

# Export
openpyxl==3.1.5

# NOTE: Removed unused packages:
#   djangorestframework_simplejwt — project uses Token auth, not JWT
#   PyJWT — was only needed for simplejwt
#   drf-spectacular — optional API docs (add back if needed)
#   django-environ — replaced with python-dotenv
